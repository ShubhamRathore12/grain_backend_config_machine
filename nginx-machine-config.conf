# Nginx location block for machine-config-service
# Add this INSIDE the existing server block in /etc/nginx/sites-available/primeosys.com
#
# location /machine-config/api/ {
#     proxy_pass http://127.0.0.1:8080/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection 'upgrade';
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_cache_bypass $http_upgrade;
# }
#
# After adding, test and reload:
#   nginx -t && systemctl reload nginx
#
# Public URL will be: https://www.primeosys.com/machine-config/api/health
